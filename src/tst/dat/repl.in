"Hello, World! \"String Test\""
; Comment Test
; Errors are not tested as their output format is subject to arbitrary change
; One the lisp interpreter becomes more advanced, I could use the meta programming
; features to simplify the testing somewhat.
; 
; Each new primitive operation should be tested when it has been added to the
; Interpreter, the corresponding output file will need to be updated as well in
; that case.

(define minus_one -5)
(define pos_number 42)
(define hello_world "Hello World")
(set pos_number 69)

; Addition
(+ 4 3)
(+ -1 minus_one)
(+ -1 -0)
(+ -1)
(+ 1)
(+ 1 2 3 4 5)
(+)

; Subtraction

(- 2 2)
(- 2)

; Multiplication
(* 2 2)
(* 1 2 3 4 5)

; Misc
(= 1 2)
(= 1 1)
(begin 1 2 3 4)
(if () 2 3)

; Strings
(scar hello_world)
(scdr hello_world)
(scons (scar hello_world) "ello Guvneh!")
(reverse hello_world)
